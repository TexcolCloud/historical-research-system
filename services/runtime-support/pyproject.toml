[build-system]
requires = ["setuptools>=75"]
build-backend = "setuptools.build_meta"

[project]
name = "hrs-runtime"
version = "0.1.0"
description = "Shared S3 storage, GPU scheduling and source-layout helpers"
requires-python = ">=3.11"
dependencies = ["boto3>=1.40,<2"]

[tool.setuptools.packages.find]
where = ["src"]

[tool.ruff]
extend = "../../ruff.toml"
target-version = "py311"
line-length = 100
