# 审核后自动入库与分件指引（2026-09-10）

## 根因与修复

- 人工放行后的入库已经成功；首次导入曾将成功回执停留在 `submitted`。现在仅在导入任务 completed 且 receipt.adoption_state=current 时记录 published；正文修订仍使用独立采用应用回执，不将旧 import receipt 的 pending_adoption 误判为当前状态。
- 提取模块输出载体来源、article_id=null 和 boundary_evidence。旧入库规则只接受 article_id/旧 layout_group_id，导致所有载体页未分件。
- `partition-rules-3` 在入库模块消费起篇/结篇与连续物理页线索形成候选。无跨页断句提示不等于文章终止；页号中断、附件/导航范围以及起篇前无归属内容不强行接入。旧 v1/v2 回执与缓存独立保留。来源记录、正文和人工放行记录不变。
- `GET /api/v1/extractions/{extraction_id}/partition` 回读该固定提取的最新分件任务；显式恢复使用已有 `POST /partition-runs`，固定 extraction_id、rule_version 和幂等键，不重新 OCR 或导入。
- 前端显示正文待审、自动入库、篇目划分三个独立状态，直接进入已入库正文/分件草案/分件任务。入库列表定时刷新；展开分页后暂停自动刷新，避免冲走已加载条目。空列表解释下一步。组织草案先显示候选篇目和操作指引，复杂 JSON 编辑折叠保留。

## 验证与当前运行限制

- 单测先复现首次入库误标 submitted、载体边界未消费、无断句提示的连续页被遗漏，以及部分放行提示错误，再验证修复。
- 隔离 PostgreSQL 的输入准备、导入、自动分件、草案预览/应用回归通过。测试不使用真实史料或模型。
- 前端构建和 35 项测试通过；现有编辑器大包警告保留。
- 4 项 S3 用例在输入准备阶段失败，未作为成功验证计入；本次本地存储链路测试通过。
- 浏览器实测：入库状态与来源载体对应，分件草案入口可打开且有下一步指引。截图在 `output/playwright/ingestion-flow-20260910/`。
- 真实 PDF `458128a1-aaea-4c65-836f-682beda76a55` 第 6 轮仍为 published、待审 0，未更改正文或人审记录。第一轮修复的 v2 分件任务 `729bc022-bd9f-45ae-8e37-f9f9db73be3e` 已完成，形成 19 个候选和 105 段待定；这些不是最终范围验收。
- **最终 v3 连续页修复尚未加载到真实入库服务/worker**：第二次重启命令被执行策略拦截，未绕过。需要先停止当前入库 API 和 worker，再按原配置启动；随后仅针对当前 extraction `e3d88882-43a5-4edb-82c0-0c935828b43c` 提交 v3 分件，幂等键 `workbench-partition-v3-e3d88882-43a5-4edb-82c0-0c935828b43c`，回读结果。

## 不改变的决定

正文待审清零无需再次人工放行，不触发机器复审；局部待审与依赖保留。篇目候选不等于已确认身份，分件歧义不撤销正文入库。未启用全库自动制卡、未应用任何真实分件草案，未修改冻结的文档提取模块。此次不宣称全研究链路验收完成。
