# 转换接入验证记录

日期：2026-09-10。范围：桌面 React/CSS 工作台与本机转换适配服务；不是 OCR 准确率、史料语义或全研究链路验收。

## 项目管线真实运行

用户指定 `_117-157.pdf`，41 页，3,292,270 字节，源 SHA-256 为 `f119e6f1448515f0198f781f1f0dc2cba204ea79720aa3a97405fe16d8fde8ec`。

任务 `d7fb8acc-343b-4a13-b64a-16e496c6df44` 第 1 轮已完成。项目报告：Docling 2.123.0、PDFium、PaddleOCR-VL，41 次识别，0 次 OCR 失败，0 缺页；DeepSeek Flash 41 份 completed 复核回执。转换约 687.8 秒，语义复核约 75.5 秒，管线合计约 763.3 秒（端到端含进程启动约 13 分钟）。

管线 readiness 输出 201 个 usable 内容块、0 个待审块；`human_review=false`、`article_structure_status=not-claimed` 原样保留。以上均是项目机器输出，没有 GPT/Codex 补写、修改、替代复核或放行；不据此承诺逐字准确或史实真实。未提交真实材料入库或制卡。

结果位于项目 `state/workbench-conversion/d7fb8acc-343b-4a13-b64a-16e496c6df44/attempts/1/`。原始 PDF、逐页原图、Docling 初稿、模型回执、最终 Markdown、可用性和来源映射均保留；具体证据摘要见 `output/pdf/workbench-conversion-20260910/integration-verdict.json`。

## 工程验证

- 26 项前端单测和 TypeScript / Vite 构建通过。
- 5 项适配服务测试及 1 项独立 PostgreSQL 的实际产物入库测试通过；无历史库写入。
- 新增 10 项浏览器隔离交互、原有 15 项非转换模块回归通过。夹具响应全部拦截，不交给真实模型。
- 真实页面只读检查：完成状态和真实内容块、原图解码、最后物理页回读、无待审块空状态，共 4 项通过；API 第 41 页返回 9 个块和与产物相同的正文摘要。
- 测试先暴露缺少上传方法/控制路由/执行器/打包方法/状态映射，再实现后通过。浏览器首轮因原文与只读源码重复文本导致定位器不唯一，改为定位渲染容器后通过；不是更改业务结果。

## 界面检查（full，开发机器检查）

沿用已确认的桌面布局和 CSS token，不另引入 UI 框架。检查顺序为原件/工程夹具定义在先，任务与结果页面在后；只评价界面工程，不评价或更改真实史料。

| 类别 | 检查证据 | 结果 |
| --- | --- | --- |
| 字体与信息层级 | 任务长文件名、轮次、物理页、状态标签和内容块截图 | 长名换行；完成与人工通过分开 |
| 表面与操作区域 | 原生上传/确认、三栏结果、选中块、只读明细、错误/空状态 | 待审及当前块使用既有标记色并附文字；图片使用真实页图 |
| 动效 | reduced-motion 下浏览器回归；本次没有新动效 | 不做装饰加载动画；10% 慢速动效回放未验证 |
| 图标 | 复用现有 SVG 侧栏 | 未增加新图标族 |
| 性能 | 构建、1366/1440 桌面回归、逐页结果请求 | 无页面横向溢出；编辑器 702 kB 功能包告警仍存在，未做性能负载验收 |

| 严重性 | 位置 | 原先 | 实现后 | 原因 |
| --- | --- | --- | --- | --- |
| MEDIUM（已处理） | `src/live/LiveApp.tsx`、`conversion-status.ts` | 转换列表直接显示后端 running/completed | 共享中文状态标签，未知状态保留原值 | 列表与详情一致，转换完成不暗示人审通过 |

考虑但未采用：替换已确认的三栏布局（不属本轮目标）；添加估计完成百分比（CLI 不提供依据）；用 VLM 占位框绘制精确错误矩形（仅能证实页级定位）。

开发界面结论：Approve，仅限已检查的桌面接入范围。未验证项：完整屏幕阅读器、200% 缩放、超大文档压力、多进程恢复、公网鉴权、正文编辑放行闭环。G04/G05 仍为功能缺口，不计为本次已完成。机器检查绝不写入转换产物或生产审核状态，最终用户验收待用户进行。
