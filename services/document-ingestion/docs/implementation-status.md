# 实施状态

设计依据是项目的“史料入库与文档组织：首版实施决策地图”及其已解决决策。
本文件区分阶段性实现和完整首版验收，不将设计完成写成代码完成。

## 已约定的检查入口

沿用用户已确认的接口与验收设计，TDD 检查通过实际 HTTP、薄 CLI、
模块公开用例及存储接口进行；数据库行为使用隔离 PostgreSQL，不以 SQLite 替代。
不重新确认已经选定的入口，不通过测试私有函数或伪造业务数据库行证明导入完成。

## 当前阶段

检索实施期间，经用户于 2026-09-08 授权新增按证据范围的机器用途复核，迁移头为
`0015_source_qualifications`。公开上下文、草案预览／采用、用途计算、撤销和变更事件已接通。
相关隔离 PostgreSQL 回归为 **8 passed**，见 [资格与用途回归](../evaluation/retrieval-qualification-final-v1.xml)。
该扩展保留原始 readiness、正文和 DeepSeek 生产路由；下文 90 项回归及原首版汇总属于扩展前的入库基线，
不能直接视为新源码的全量复验。扩展的实际检索联动与机器审阅随检索模块验收归档。

### 扩展前的入库基线（2026-09-08）

版本 `0.1.0` 完整首版实现与机器验收完成；最新代码已于 2026-09-08 完成集成复验。输入传输、正式来源、规则候选、DeepSeek 辅助、清单编辑、文献身份、复杂组织、用途核查与下游增量均有可运行链路。当前终态为**待用户最终验收**，不是生产上线或史料人工审定。
任务/导入/载体/草案/文献/组织均可分页浏览；文献支持书目说法、状态和载体筛选。在线修订追加不可变历史，待定/拒绝候选可通过新修订重新开启，不继承旧批准。
迁移头为 `0014_assistance_budget`，专用开发库已升级，重复升级 changed=false。本轮核心回归 **90 passed、0 skipped、2 warnings，1041.55 秒**；另承接 2026-09-07 的真实大包 **4 passed**、DeepSeek 工程实调用 **1 passed**，后二者本轮未重跑。Linux 按当前源码重建镜像后，双后端链路、中文路径、原生 CLI 和重启回读重新通过。Ruff、格式、编译及契约回归通过。
现役入口为[首版验收说明](first-version-acceptance.md)与[带哈希的复验汇总](../evaluation/v1-recheck-20260908/acceptance-summary.json)。原验收基线保持不变；汇总校验核心运行与当前源码、Linux 镜像相符，并明确区分新执行和沿用证据。下文“历史阶段”仅描述当时状态，不是当前待办。
传输准备、来源合同通过、当前技术快照和研究用途放行仍是不同状态，不能互相代替。
首版全部要求仍包括双存储、tus、复杂组织、分层修订和完整验收，未做范围删减。
尚未验收的能力不能通过占位接口、模拟存储或旧原型报告宣称已完成。

### 历史阶段：草案与复杂阅读组织推进记录（2026-09-07）

- 草案、不可变修订、预览和采用任务有真实 HTTP 接口。预览在可回滚事务中验证拟议结果，不发布临时目录或临时快照；客户端不能提交自造的“已批准”组。
- 服务端根据新建引用、实际读写目标、同级位置和已有/拟议来源范围计算依赖连通组。选定组原子发布，独立组分别处理；过期输入只阻止受影响组，旧采用重放不会重置当前结果。
- 已实现暂定文献、独立历史版本和收录位置，以及有方向、依据、状态的版本关系；相同题名不自动归并。书目断言可同时保存多个说法，当前采用值与全部旧修订分别可读。
- 多层目录明确区分导航与独立篇目。节点修改、互换、拆合按最终目录验证并留下新修订；没有来源的独立篇目、循环、未解释的同级范围重叠、悬空附属归属不能直接采用。
- 附属内容可待定或显式共享，保存受影响实例及用途限制声明；此阶段尚未把这些声明接入研究读出资格计算。
- 阅读组合引用固定来源快照、码点范围和文字摘要；支持接续、显式缺口、替代扫描、另一版本补配。原缺口与旧来源仍可读，跨历史版本标识和版本归属是否齐备分别返回，不修改原始 Markdown。
- 全量回归 **53 passed、0 skipped、2 warnings，115.37 秒**，包括五页真实 PDF 及本地/S3 小包链路；新增八项草案/复杂关系测试均走真实 PostgreSQL。开发库未声称升级；`0006_drafts` 至 `0010_reading_attribution` 仅在隔离测试库应用。
- 机器审阅先复看原 PDF 首页与工程原图，再检查新的来源和拆合回执。工程字节/关系测试不是历史事实或中文字形证据；没有人工批准、金标或最终首版验收。

证据：[全量回归](../evaluation/organization-regression-20260907.xml)、[真实来源回读](../evaluation/organization-real-source-20260907.json)、[工程拆合记录](../evaluation/organization-relations-20260907.json)、[原件优先机器审核](../evaluation/organization-gpt-review-20260907.json)。这些是阶段证据，未替代下列剩余首版项目。

### 历史阶段：双存储、传输与薄 CLI 推进记录（2026-09-07；不是首版最终验收）

- `pack` 无须数据库，按明确引用收集正文、页图、表格审核证据，生成 ZIP64 与包装清单，保留上游字节；支持显式原文件或接收端已有原件资产，以及明确选定的诊断附件。
- 已启动固定摘要 SeaweedFS 4.45 专用验收容器与持久卷，仅绑定本机 58333。没有停启 Docker Desktop；引擎恢复后只启动了已有专用 PostgreSQL 容器。真实本地/S3 写入、完整 SHA-256 读回、复用、分段上传回执及数据库发布失败后的原位置恢复已测试。
- HTTP 已有资产元数据、位置/写入记录与应用转发 GET/HEAD/单段 Range、ETag。S3 Range 传至对象服务，不先下载整文件再截取。
- `uploads` 提供持久会话、不可变核验块、短事务偏移发布，以及 tus 创建/HEAD/PATCH/Checksum 和管理读取。ZIP 上传完成仍仅是传输齐备；准备阶段逐块重组、校验整包、检查成员并进入与目录输入相同的冻结流程。
- 薄 CLI 已支持 ZIP、服务端接收目录、已有 prepared input 三种提交入口；新载体和明确已有载体分别使用 ingest/reextract。通过服务器 capabilities 选择块大小，持久保存非秘密回执。`jobs show/wait/cancel/retry` 共用 HTTP；等待超时不取消，控制回执绑定原执行代次。
- 本阶段完整回归 **45 passed、0 skipped、2 warnings，90.48 秒**，包含启用真实五页原 PDF 的 11 个来源范围精确回读。本地/S3 均已通过小包上传至标准入库链路；独立 TCP/worker/薄 CLI 已验证提交及重放。
- 原件优先的 GPT 开发机器审核再次复看真实原 PDF 首页后比对当前回读。单像素工程图没有文字字形证据；工程表格与传输用例不是历史真实性、人工复核或金标批准。
- `0005_uploads` 已在测试隔离库中验证；此记录不声称专用开发库已升级到新迁移头。旧失败回归保留在 `transport-regression-20260907.xml`，其中的旧“上传接口不存在”断言已被实际 tus 合同断言替换，后续完整回归通过。

证据：[全量回归](../evaluation/transport-green-regression-20260907.xml)、[真实来源回读](../evaluation/transport-real-source-20260907.json)、[原件优先机器审核](../evaluation/transport-gpt-review-20260907.json)。这些报告描述生成时的代码，不代替后续组织模块和最终验收。

### 历史阶段：正式来源入库与固定回读（2026-09-07）

- `/imports` 使用既有持久任务与本地资产存储，独立读取冻结的 4/1/1/1 提取合同，不调用提取模块内部代码。
  输入指纹、原始来源绑定、正文摘要、Unicode 码点范围、来源指针文本、页记录及 readiness 依赖均作基础校验。
- 标准合同失败保留冻结文件和拒绝回执，发布事务回滚，不留下半份正式结果；旧格式只作 `archived_pending`。
  原 PDF/输入图片必须显式绑定，不能从上游绝对路径自行取文件；不伪造缺失映射或提升内容状态。
- 载体、正文和证据修订分别登记。正文一致可复用；JSON 排版差异可复用证据，未知证据变化保守生成新修订。
  首个新载体产生技术来源快照，已有载体的新结果待采用，不自动切换当前输入，不依据同字节归并历史身份。
- 原页、图像每次收录、表格结构及相关显式引用分别记录。实际补测发现跨页引用的旧唯一约束过窄，
  新增 `0004_table_context`，保留物理页和引用包路径上下文；专用开发库已经应用此迁移，不删除原数据。
- 已提供提取详情、分页页/资产列表、载体当前输入、快照详情/片段与只读固定截取接口。
  本阶段不实施自动制卡或研究用途计算，返回 `current_usage_evaluated:false`。
- 真实五页提取包完整入库并回读 11 个来源片段，文本、页序与来源摘要一致；50 项待补证、3 项非来源生成内容原样保留。
  真实包无内嵌图片或表格；图片重复收录、表格引用链与跨页共用使用明确标注的工程构造用例，不能替代真实表格验收。
- 主代理先看原 PDF 首页渲染，再看回读结果；GPT 开发机器审阅核对可见标题、署名和物理页来源。
  审阅只批准这一范围的入库保真检查，不宣称全文 OCR 正确、史实审定、人审、金标或最终验收。
  单像素工程图片只用于资产/坐标协议，明确没有中文字形证据。未调用 DeepSeek 或额外外部评审 API。
- 本轮 TDD 实际观察到标准读取、拒绝回执、必需表格缺项、引用链、当前快照读取和跨页共用的失败再通过。
  目前完整回归为 **26 passed、0 skipped、2 warnings**，包含显式启用的真实材料测试；未配置真实材料路径时该项会跳过。

当前证据：[检查记录](../evaluation/source-import-checks-20260907.json)、
[JUnit](../evaluation/source-import-tests-20260907.xml)、[真实来源回读](../evaluation/real-source-import-20260907.json)、
[GPT 开发审阅](../evaluation/source-import-gpt-review-20260907.json)。
测试使用并删除各自生成的临时 PostgreSQL 库，不将报告中的临时 UUID 当作持久开发库记录。
旧阶段报告及其旧构建物保留为历史，不代表当前代码版本。

### 历史阶段：数据库基线与本地输入准备（2026-09-07）

- 全部实现仍在 `services/document-ingestion`。固定 PostgreSQL 18.6 镜像及摘要，
  新建专用 Compose 容器、网络、持久卷及 `ingestion_dev` / `ingestion_test`，未修改原有数据卷。
- 三份 Alembic 迁移已实际应用到开发库，迁移头 `0003_organization`；重复升级不重复创建数据。
  底表覆盖资产/位置、任务/尝试、载体、正文/证据修订、文献/版本/收录位置、书目断言、层级组织和读取快照。
  这些是关系约束基础，不代表复杂组织确认、分层修订采用或不可改历史流程已完整实现。
- `AssetStore` 本地保存先核验输入、复制时再核验、完整读回后登记已核验位置；稳定资产标识可供 Python 读取。
  错误摘要/长度声明不会污染后续正确资产；新文件目前需要两次输入读取加一次归档读回。
- HTTP 可提交本地目录准备、查询任务/冻结清单、取消及重试。独立 PostgreSQL worker 使用租约与尝试标识，
  失去租约的旧 worker 不能发布准备结果；旧取消请求重放不作用于新执行轮次。
- CLI 已提供 `serve`、`check`、`db-upgrade`、`worker [--once]`、`export-contracts`；
  尚无远程提交、打包或清单管理命令。已生成仅含已实现端点的 OpenAPI 和包装清单 JSON Schema，
  详见 [接口说明](api.md)。
- 真实 TCP 检查经过 HTTP 提交、HTTP 进程退出、独立 worker 处理、重开 HTTP 查询结果的链路。
  另已检查源目录改变不改变冻结字节、校验失败后修复重试、取消与过期租约恢复。
- 该阶段完整测试为 **18 passed、0 skipped、2 warnings**，ruff 检查及格式检查通过。
  测试数据库均为测试夹具新建的隔离 PostgreSQL，完成后只删除自己的临时库；未清理开发数据。
- TDD 对迁移、就绪检查、资产保存、输入准备、取消回放及契约导出观察到失败后再实现；
  后补的已有恢复行为检查作为回归测试记录，不伪称全部都曾经历功能 RED。
- 本阶段没有实际史料、图片、版面或候选质量评审，只有明确标注的工程字节/DDL 用例；
  没有调用 GPT 或 DeepSeek，也没有将工程测试结果记作机器审定、人审或金标。
- 本次临时开发数据库密码曾在早期失败测试的夹具展示中出现，已实际轮换该新建角色密码，
  并将测试夹具改用脱敏类型；不在报告中记录任何密码，未改动用户既有凭据。

阶段检查见 [机器可读记录](../evaluation/input-preparation-checks-20260907.json) 和
[JUnit 报告](../evaluation/input-preparation-tests-20260907.xml)。本阶段不代表首版最终验收。

### 首版验收覆盖与后续边界

- 五页真实输入回读 11 个固定范围；九页输入回读 70 个范围、2 次图片收录和 106 条表格证据引用。source-map/readiness、正文片段和文件摘要逐项核对，原始受限状态不提升。
- 规则自动子任务与导入同事务登记，同证据/规则版本去重；参考文献、导航、附属内容保留待定，DeepSeek 仅明确请求时调用。显式未知归属不回退版面分组，目录和对照摘要保留待定；没有文章归属字段的旧版面提示继续兼容。新增双存储集成检查已纳入核心回归。缓存包含模型配置，累计每包预算持久化，失败不影响规则草案。
- 清单导出/导回、在线修订/历史、文献归并/拆分/撤回、书目多说法、多层导航/独立篇目、附属待定/共享、阅读接续/缺口/替代/跨版本补配均可通过同一草案入口完成。
- 依赖组预览/部分采用、旧执行拒绝、失败恢复、即时用途限制、固定研究基准与已提交增量事件已有回归；本地/S3 原位置恢复、缺失位置重建、multipart 中断恢复和完整重核验已有实测。
- Windows 主环境和 Linux 容器均使用独立 Python 3.11、固定 PostgreSQL/SeaweedFS 及锁定依赖。Linux 原生 CLI、中文路径、双存储和持久状态重启回读已验证；不是云端部署证明。
- 首版验收已完成：ZIP64 包及单成员都超过 4 GiB，在 >4 GiB 已确认前缀处中断并重启恢复；两后端完整回读通过，S3 容器重启后再次完整核验通过。定期核验可调用已实现 reverify，不另建全库定时扫描服务。
- 原件优先 GPT 审阅已保存哈希与结构化结论；本轮纠正旧审阅对五页原件题名的误读，其候选标签与可见题名一致。九页混合中英文的临时题名仍待核对；任何候选标签都不是正式书目批准。冻结的提取输出未被本模块修改。
- 统一前端、公网认证/多租户、真实云厂商验证、向量/制卡/LangGraph，以及全部模块完成后的项目级边界检查明确属于后续范围，不是本模块的未交付接口。
- 用户最终接受、史料人工审定和 sealed gold 均未代签。历史报告位于 `evaluation/`；现役复验集中在 `evaluation/v1-recheck-20260908/`，`evaluation/v1-20260907/` 是保留的原基线；报告内的隔离数据库 UUID 不是永久开发库数据。

### 历史阶段：启动骨架

- 独立 Python 3.11.15 环境及 uv.lock 已解析安装；没有复用提取模块环境。
- /api/v1/health/live、明确返回未就绪的 /api/v1/health/ready，统一问题回执和请求标识。
- history-ingest serve/check 只提供已实现的启动/诊断能力，尚无入库管理命令。
- TDD 分别观察到缺少 HTTP 模块、缺少 readiness 路由、缺少 CLI 导致的失败，
  再完成对应实现；pytest 最终为 3 passed，ruff check 通过。
- 安装后的 history-ingest check 实际返回 database_not_configured、503 问题体及退出码 1；
  这是准确的未就绪结果，不是数据库已经连接的证据。
- 测试依赖产生两条上游弃用警告（Starlette 的 httpx 兼容路径和 AnyIO 别名），
  已如实保留，未通过过滤警告冒称无警告。机器可读检查摘要见
  [启动检查记录](../evaluation/bootstrap-checks-20260907.json)。

### 历史环境记录：Docker 升级与 CLI 停启故障

2026-09-07 用户明确授权“修复 docker 全局配置”。前一阶段的启动阻塞仍保留在
[历史启动检查](../evaluation/bootstrap-checks-20260907.json)，不将当时的 blocked 改成通过。

- 旧日志曾报告 settings-store.json 含 NUL；本轮实际读取时它已经是有效 UTF-8 JSON，
  因此只备份，没有手工重置。不能将该文件先前的损坏归因或恢复归功于本轮操作。
- 随后的启动日志分别指向 .docker/daemon.json 和 .docker/windows-daemon.json；
  实检为 124/124、28/28 字节全部为 NUL。停机、复制并校验 SHA-256 后，分别恢复为
  默认空 JSON 对象。Docker 随后自行补齐 Linux 默认配置；原自定义值无法从全零内容恢复。
- 配置解析通过后，运行时 socket 又阻塞启动。仅将已确认全为零字节通信节点的
  Docker/run 与 docker-secrets-engine 目录改名保留，未清理镜像、容器或数据卷，
  未读取密钥内容，也未重置 WSL 或恢复出厂设置。
- 用户随后明确授权升级到 4.89.0。固定版本官方安装包通过 SHA-256 和 Docker Inc
  数字签名校验，安装器退出 0，安装位置仍为 C:/Program Files/Docker/Docker，
  程序版本为 4.89.0.238018；没有切换安装模式或后端。
- 升级前停机备份三份配置和 Docker 自己的两个 VHDX，共 24,385,683,755 bytes
  （约 22.7 GiB），逐项 SHA-256 与源文件一致。备份保留在
  C:/Users/23835/AppData/Local/Docker/upgrade-backups/4.87.0-to-4.89.0-20260907。
- 升级并未消除本机的 CLI 停启故障：一次启动可以成功，随后执行
  docker desktop stop --timeout 30 再启动，会在重命名旧 socket 时崩溃。
  既观察到 Secrets Engine 的 engine.sock，也观察到 Ingest server 的
  sailor-ingest.sock。移开旧版遗留目录后，新版新建的 socket 也复现，因此不能
  仅解释为升级前残留，不能报告两轮停启通过。
- 较早的 API-only 检查和“仅启动偏慢”解释不足以证明整体正常，已被后续的
  Desktop 状态与崩溃日志联合检查取代。没有确认 Windows 内核层根因，
  未执行原本拟议的隔离 socket 探针，也未重启 Windows、改驱动或重置 WSL。
- 用户说明正常启动后是最新命令触发崩溃；已停止反复停启验证。最后仅将已停机且
  确认全为零字节通信节点的两个运行目录改名保留，然后启动一次并保持运行。
- 14:19:35 +08:00 最后核验：Desktop status 为 running，Linux Engine 29.7.2
  API 可用，实际 Desktop 为 4.89.0；0 个容器、0 个镜像及原来的同一个数据卷可见，
  最后一次恢复启动后的日志未发现新增后台崩溃。14:20:18 三份配置均为有效 JSON、无 NUL。
  这是当前可运行的检查，不是停启故障已解决或卷内数据逐文件等价的证明。

#### 后续只读诊断（2026-09-07 14:35 +08:00）

- 没有再执行 Desktop 停止、启动或重启，也没有修改系统配置或运行目录。
- 轮转日志显示上次停止记录了 ExitHealthyState，监控器记录 services 子进程退出 0；
  CLI 也在 30 秒期限前退出 0。下一次启动才在 sailor-ingest.sock 重命名处失败，
  因此不支持“CLI 等待超时强杀造成此次失败”的解释。退出时另有 frontend 的
  Access is denied 警告，但随后记录 electron processes have shut down，不能仅据此认定根因。
- 当前查询返回 stopped，但同一会话的 Desktop 和 backend 进程仍在；14:24:56 起日志
  明确记录 idle mode，随后为 Idle:shutdown、DockerAPI:running，没有恢复后新增的崩溃行。
  这次 stopped 是空闲资源节省状态，不应误判为再次崩溃；没有为验证而唤醒引擎。
- 故障范围已定位到 Windows 宿主侧 socket 清理／重新建立；尚未确定句柄持有者或
  内核层根因。没有新增自动清理／重启脚本；进一步有中断的系统诊断需用户另行决定。

升级、完整备份、失败回归及最终恢复结果见
[Docker 升级记录](../evaluation/docker-upgrade-20260907.json)；
[先前配置修复记录](../evaluation/docker-config-repair-20260907.json) 保留为历史快照。
上述 Docker 诊断阶段没有新增数据库模型、迁移或入库逻辑；其后的模块实现进度见本文件“当前阶段”。
模块实施期间没有再停启 Docker Desktop，不能用 Docker 已启动代替模块验收。

## 保护范围

本模块使用独立 Python 3.11 环境、依赖和配置，不导入 document-extraction 内部代码。
现有提取模块未提交改动属于用户，保持不变；不运行 OCR 或表格调优。
