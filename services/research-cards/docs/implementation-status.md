# 实施与验收状态

2026-09-09：工程初版 `0.1.0` 已实现，随后按用户要求加入 Pro 主 agent／Flash 子 agent，当前工程版本 `0.2.0`。交付范围仍遵循“不用完成全部验证”“只要工程完成初版即可”：可运行代码、显式接口、基本工程检查和可恢复产物；完整质量验收延期。

实现依据为[首版实施规格](../../../.scratch/research-cards/spec.md)及其中的完整决议。原验收设计保留为后续计划，不将本次开发运行记作正式通过。

## 功能状态

| 功能 | 工程状态 | 主要代码 |
| --- | --- | --- |
| 独立 Python 包、配置、PostgreSQL 迁移与持久记录 | 已实现 | `settings.py`、`database.py`、`store.py` |
| 全文读取、阅读记录、长文摘要、补查、卡片组装、原图及语义核查 | 已实现，真实开发运行已产出候选 | `generation.py`、`digests.py`、`supplement.py`、`vision.py` |
| Pro 委派阅读、Flash 执行、Pro 接收；Pro 选择有界补查 | 已实现工程初版，上下文系统待定 | `subagents.py`、`generation.py`、`supplement.py`；见 [subagent 说明](subagents.md) |
| 不可变版本、人工保护、成组预览与采用 | 已实现 | `revisions.py`、`adoption.py` |
| 题目、固定依据的论文提示及更新记录 | 已实现 | `paper.py`、`catalog.py` |
| 自动来源事件、补建、增量影响、有限发现与续接 | 已实现 | `source_sync.py`、`impacts.py` |
| 双执行单元、队列、批任务、预算、停止与故障恢复 | 已实现 | `queue.py`、`worker.py`、`metering.py` |
| HTTP／Python／CLI、索引、导出、备份恢复 | 已实现 | `api.py`、`client.py`、`cli.py`、`index.py`、`exports.py`、`recovery.py` |
| 24 篇正式范围、三路比较、8 条真实增量序列、论文提示质量及 Linux 实机验收 | 延期 | 保留设计与已产生材料，本轮不继续 |

## 已执行的工程检查

- `0.2.0` 本次完整工程回归 40 项通过；随后对主 agent 工具轮恢复顺序的修正补跑 3 项 subagent 用例并全部通过。此前初版累计覆盖 37 项。不把工程用例计为史料质量样本。
- 使用真实 PostgreSQL 检查幂等回执、并发派发、租约、暂停与预算、不可变修订、保护和固定引用。检查了真实本地 OpenSearch 索引延迟及重建，以及 HTTP、Python 和 CLI 导出的字节一致性。
- 真实 SDK 连接本地 SSE 协议夹具，检查输出截断、结构失败、未知使用量和可恢复结果；最后新增用例确认对象编号错误只进行一次格式修正，实质负面结论仍保留。
- 已有真实 DeepSeek Flash、Pro、Vision 的开发协议记录和候选产物；原件优先的 GPT 机器评阅与证据哈希保存在 `evaluation/development/`。
- 已对实际校准数据库及附件执行备份和隔离恢复，保留 3 个既有任务状态、累计预算和用量，校验 121 个附件后启用恢复环境。工程夹具另覆盖缺失或损坏附件及旧版本引用。
- 当前 C01 已在正在执行的单元保存后暂停，预算预留为零；恢复环境工作进程已退出。没有继续派发校准或正式质量样本。
- 初版曾导出 54 份契约并构建 `0.1.0` wheel。本次新增 3 份 subagent 数据契约，共导出 57 份 OpenAPI／JSON Schema 文件；安装包为 `dist/historical_research_cards_service-0.2.0-py3-none-any.whl`。原版包保留。

## 已知限制与后续入口

1. 开发材料中仍有引用逐字校验、JSON 格式失败及单批输出量过大的情况，已保存错误和消耗；尚未证明长材料稳定完成率。新增 subagent 编排只完成工程验证，尚未重跑这些史料，因此不声称上述质量问题已被新策略解决。后续可从 `subagents.py`、`generation.py`、`digests.py` 和 `prompts.py` 进入。
2. 文字资格和图像资格分别判断。C07 初次运行读取文字完整，但因图像字段尚未准用而未采用。C07／C08 后来已通过上游公开 API 补齐机器评阅的图像字段资格；按最新交付范围未继续重跑。
3. 当前候选存在重复说明、书志及版式观察未充分进入研究文字等质量问题。GPT 内容评价未替代生产核查，未通过的候选未被记为已采用。
4. 全量来源资格整理、正式保留集、质量阈值、三路与增量对照、完整成本比较、Linux 实机和项目级边界加固仍未完成。没有正式质量通过率或 token 节省结论。
5. 已提交校准脚本和旧记录用于追溯，部分脚本固定了开发样本、端口和输出目录；请从回执及状态开始恢复，不要批量重放这些脚本。

入口为[模块 README](../README.md)、[运行与恢复](operations.md)和[契约清单](contracts/manifest.json)。生产仍沿用配置中的 DeepSeek；开发 GPT 记录均为机器评阅，不是用户人工审定或 sealed gold。本模块读取既有提取产物，没有加入 OCR 或表格调优逻辑。
